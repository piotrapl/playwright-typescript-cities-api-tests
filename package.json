{
  "name": "pwrt.ts.api-miasta",
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "@playwright/test": "^1.45.0",
    "typescript": "^5.0.0"
  },
  "scripts": {
    "test": "playwright test",
    "test:report": "playwright test && playwright show-report"
  }
}